# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.production

# Build outputs (if adding build process later)
dist/
build/
*.cache

# Temporary files
tmp/
temp/
*.tmp

# Node modules (if adding npm later)
node_modules/

# Package manager locks (keep for now, but can ignore if needed)
# package-lock.json
# yarn.lock
